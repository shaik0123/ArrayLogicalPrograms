using System;

namespace ArrayLogical
{
    internal class Program
    {
        static void Main(string[] args)
        {
             Console.WriteLine("Hello World!");
             DuplicateElements.Count();
             UniqueElements.Unique();
             MinimumElement.Min();
             MaximumElement.Max();
             ElementFrequency.Frequency();
             Pattern.Star();
             PatternTwo.Square();
             WordReverse.Word("this is a car");
             SumOfNUmber.Num(456);
             Matrix.Sum();
            PrintValues.Values(1);
        }
    }
}
